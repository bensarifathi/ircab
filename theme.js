import { extendTheme } from '@chakra-ui/react'

const theme = extendTheme({
  fonts: {
    heading: 'poppins',
  },
  colors: {
    ircab: {
      100: '#FE504F',
      900: "#1a202c",
    }
  }
})

export default theme